#include <stdio.h>
/* betty style doc for function main goes there*/
/**
 * main - is the main function
 *
 * Return: always return 0
 */
int main(void)
{
	char i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
